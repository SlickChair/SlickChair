# --- Created by Slick DDL
# To stop Slick DDL generation, remove this comment and start using Evolutions

# --- !Ups

create table "TASKS" ("id" BIGINT GENERATED BY DEFAULT AS IDENTITY(START WITH 1) NOT NULL PRIMARY KEY,"label" VARCHAR NOT NULL);
create table "TOKENS" ("UUID" VARCHAR NOT NULL PRIMARY KEY,"EMAIL" VARCHAR NOT NULL,"CREATION_TIME" TIMESTAMP NOT NULL,"EXPIRATION_TIME" TIMESTAMP NOT NULL,"IS_SIGN_UP" BOOLEAN NOT NULL);
create table "users" ("USER_ID" VARCHAR NOT NULL,"PROVIDER_ID" VARCHAR NOT NULL,"EMAIL" VARCHAR,"FIRST_NAME" VARCHAR NOT NULL,"LAST_NAME" VARCHAR NOT NULL,"AUTH_METHOD" VARCHAR NOT NULL,"AVATAR_URL" VARCHAR,"HASHER" VARCHAR,"PASSWORD" VARCHAR,"SALT" VARCHAR);

# --- !Downs

drop table "TASKS";
drop table "TOKENS";
drop table "users";

